```mermaid
erDiagram
  USERS ||--o{ PROGRESS : "user_id"
  USERS ||--o{ COURSES : "instructor_id"
  USERS ||--o{ COMMENTS : "user_id"
  USERS ||--o{ REPLIES : "user_id"
  COURSES ||--o{ LECTURES : "course_id"
  LECTURES ||--o{ PROGRESS : "lecture_id"
  LECTURES ||--o{ COMMENTS : "lecture_id"
  COMMENTS ||--o{ REPLIES : "comment_id"

  USERS {
    int id PK
    string email UK
    string password
    string name
    int weight "nullable"
    enum role "student | instructor | admin, DEFAULT student"
    datetime created_at "nullable"
  }

  COURSES {
    int id PK
    int instructor_id FK "-> USERS.id, nullable"
    string title
    string description
    string difficulty "beginner | intermediate | advanced"
    float met_value
    string thumbnail "nullable"
    datetime created_at "nullable"
  }

  LECTURES {
    int id PK
    int course_id FK "-> COURSES.id"
    string title
    string video_url
    int duration "seconds"
    int order_num "강의 순서, DEFAULT 0"
  }

  PROGRESS {
    int id PK
    int user_id FK "-> USERS.id"
    int lecture_id FK "-> LECTURES.id"
    boolean completed "hometraining에서 사용"
    int watched_time "seconds, hometraining에서 사용"
    float progress "backend에서 사용 가능성"
    %% (user_id, lecture_id) UNIQUE 권장 (upsert 로직 존재)
  }

  COMMENTS {
    int id PK
    int user_id FK "-> USERS.id (학생)"
    int lecture_id FK "-> LECTURES.id"
    text content
    datetime created_at
  }

  REPLIES {
    int id PK
    int comment_id FK "-> COMMENTS.id"
    int user_id FK "-> USERS.id (강사)"
    text content
    datetime created_at
  }
```

## SQL 마이그레이션

```sql
-- 1. USERS 테이블에 role 컬럼 추가
ALTER TABLE users
  ADD COLUMN role ENUM('student', 'instructor', 'admin') NOT NULL DEFAULT 'student';

-- 2. COURSES 테이블에 instructor_id 추가
ALTER TABLE courses
  ADD COLUMN instructor_id INT NULL,
  ADD FOREIGN KEY (instructor_id) REFERENCES users(id) ON DELETE SET NULL;

-- 3. LECTURES 테이블에 order_num 추가
ALTER TABLE lectures
  ADD COLUMN order_num INT NOT NULL DEFAULT 0;

-- 4. COMMENTS 테이블 생성
CREATE TABLE IF NOT EXISTS comments (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  lecture_id INT NOT NULL,
  content TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (lecture_id) REFERENCES lectures(id) ON DELETE CASCADE
);

-- 5. REPLIES 테이블 생성
CREATE TABLE IF NOT EXISTS replies (
  id INT PRIMARY KEY AUTO_INCREMENT,
  comment_id INT NOT NULL,
  user_id INT NOT NULL,
  content TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (comment_id) REFERENCES comments(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```
